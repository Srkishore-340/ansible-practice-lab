server {
    listen {{ nginx_listen_port }};
    server_name {{ nginx_server_name }};

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
